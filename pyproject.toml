[project]
name = "verion"
version = "0.1.0"
description = "From security findings to security decisions."
readme = "README.md"
license = "MIT"
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.19.1",
    "argon2-cffi>=25.1.0",
    # arq: the only async-native Redis job queue (Celery/RQ are sync-first,
    # fighting rule 7's async-by-default principle). Verified per ADR-009
    # against primary sources, not memory: as of this writing it is in
    # "maintenance only mode" (github.com/python-arq/arq issue #510,
    # maintainer Samuel Colvin, 2025-10-18) — security fixes continue and
    # the repo stays open to community PRs, but no active new-feature work.
    # Not abandoned (0.28.0 shipped 2026-04-16, after the announcement).
    "arq>=0.28.0",
    "asyncpg>=0.31.0",
    "fastapi>=0.141.1",
    "pydantic-settings>=2.15.0",
    "pyjwt>=2.13.0",
    # PyYAML: serializes ZapAdapter's Automation Framework plan file. Verified
    # per ADR-009 against primary sources, not memory: org-owned repo
    # (github.com/yaml/pyyaml), current release 6.0.3, actively maintained —
    # no abandonment signal. Used instead of hand-built YAML strings so a
    # target URL containing YAML-special characters can never corrupt the
    # plan document's structure.
    "pyyaml>=6.0.3",
    # semgrep: SAST engine per PRODUCT_SPEC's tech stack. Verified per ADR-009
    # against primary sources, not memory: published to PyPI by Semgrep Inc.
    # (San Francisco security company, formerly r2c), actively maintained,
    # releases as recent as 2026-07 (v1.170.0) — no abandonment signal. The
    # PyPI wheel bundles the compiled binary, so `uv sync --locked` alone
    # makes the `semgrep` CLI available — no extra CI install step needed.
    "semgrep>=1.170.0",
    "sqlalchemy[asyncio]>=2.0.52",
    "uvicorn>=0.52.4",
]

[dependency-groups]
dev = [
    "httpx2>=2.12.0",
    "import-linter>=2.13",
    "pre-commit>=3.8",
    "pytest>=8.3",
    "pytest-asyncio>=1.4.0",
    "ruff>=0.6",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
target-version = "py312"
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "C4"]

[tool.ruff.format]
quote-style = "double"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
# Session-scoped: a session-scoped async engine (see tests/integration/conftest.py)
# needs a session-scoped event loop, or asyncpg connections created in one test's
# loop get reused in another's and fail with "InterfaceError: another operation
# is in progress". Don't remove these two settings — see CLAUDE.md's testing
# requirements section.
asyncio_default_fixture_loop_scope = "session"
asyncio_default_test_loop_scope = "session"

[tool.importlinter]
root_package = "verion"
include_external_packages = true

[[tool.importlinter.contracts]]
id = "layers-identity"
name = "identity: adapters -> application -> ports -> domain"
type = "layers"
layers = [
    "adapters",
    "application",
    "ports",
    "domain",
]
containers = ["verion.modules.identity"]

[[tool.importlinter.contracts]]
id = "layers-projects"
name = "projects: adapters -> application -> ports -> domain"
type = "layers"
layers = [
    "adapters",
    "application",
    "ports",
    "domain",
]
containers = ["verion.modules.projects"]

[[tool.importlinter.contracts]]
id = "layers-scanning"
name = "scanning: adapters -> application -> ports -> domain"
type = "layers"
layers = [
    "adapters",
    "application",
    "ports",
    "domain",
]
containers = ["verion.modules.scanning"]

[[tool.importlinter.contracts]]
id = "layers-normalization"
name = "normalization: adapters -> application -> ports -> domain"
type = "layers"
layers = [
    "adapters",
    "application",
    "ports",
    "domain",
]
containers = ["verion.modules.normalization"]

[[tool.importlinter.contracts]]
id = "layers-correlation"
name = "correlation: adapters -> application -> ports -> domain"
type = "layers"
layers = [
    "adapters",
    "application",
    "ports",
    "domain",
]
containers = ["verion.modules.correlation"]

[[tool.importlinter.contracts]]
id = "layers-risk-engine"
name = "risk_engine: adapters -> application -> ports -> domain"
type = "layers"
layers = [
    "adapters",
    "application",
    "ports",
    "domain",
]
containers = ["verion.modules.risk_engine"]

[[tool.importlinter.contracts]]
id = "layers-brief"
name = "brief: adapters -> application -> ports -> domain"
type = "layers"
layers = [
    "adapters",
    "application",
    "ports",
    "domain",
]
containers = ["verion.modules.brief"]

[[tool.importlinter.contracts]]
id = "layers-history"
name = "history: adapters -> application -> ports -> domain"
type = "layers"
layers = [
    "adapters",
    "application",
    "ports",
    "domain",
]
containers = ["verion.modules.history"]

[[tool.importlinter.contracts]]
id = "cross-module-identity"
name = "identity does not reach into other modules' domain/adapters directly"
type = "forbidden"
allow_indirect_imports = true
source_modules = ["verion.modules.identity"]
forbidden_modules = [
    "verion.modules.projects.domain",
    "verion.modules.projects.adapters",
    "verion.modules.scanning.domain",
    "verion.modules.scanning.adapters",
    "verion.modules.normalization.domain",
    "verion.modules.normalization.adapters",
    "verion.modules.correlation.domain",
    "verion.modules.correlation.adapters",
    "verion.modules.risk_engine.domain",
    "verion.modules.risk_engine.adapters",
    "verion.modules.brief.domain",
    "verion.modules.brief.adapters",
    "verion.modules.history.domain",
    "verion.modules.history.adapters",
]

[[tool.importlinter.contracts]]
id = "cross-module-projects"
name = "projects does not reach into other modules' domain/adapters directly"
type = "forbidden"
allow_indirect_imports = true
source_modules = ["verion.modules.projects"]
forbidden_modules = [
    "verion.modules.identity.domain",
    "verion.modules.identity.adapters",
    "verion.modules.scanning.domain",
    "verion.modules.scanning.adapters",
    "verion.modules.normalization.domain",
    "verion.modules.normalization.adapters",
    "verion.modules.correlation.domain",
    "verion.modules.correlation.adapters",
    "verion.modules.risk_engine.domain",
    "verion.modules.risk_engine.adapters",
    "verion.modules.brief.domain",
    "verion.modules.brief.adapters",
    "verion.modules.history.domain",
    "verion.modules.history.adapters",
]

[[tool.importlinter.contracts]]
id = "cross-module-scanning"
name = "scanning does not reach into other modules' domain/adapters directly"
type = "forbidden"
allow_indirect_imports = true
source_modules = ["verion.modules.scanning"]
forbidden_modules = [
    "verion.modules.identity.domain",
    "verion.modules.identity.adapters",
    "verion.modules.projects.domain",
    "verion.modules.projects.adapters",
    "verion.modules.normalization.domain",
    "verion.modules.normalization.adapters",
    "verion.modules.correlation.domain",
    "verion.modules.correlation.adapters",
    "verion.modules.risk_engine.domain",
    "verion.modules.risk_engine.adapters",
    "verion.modules.brief.domain",
    "verion.modules.brief.adapters",
    "verion.modules.history.domain",
    "verion.modules.history.adapters",
]

[[tool.importlinter.contracts]]
id = "cross-module-normalization"
name = "normalization does not reach into other modules' domain/adapters directly"
type = "forbidden"
allow_indirect_imports = true
source_modules = ["verion.modules.normalization"]
forbidden_modules = [
    "verion.modules.identity.domain",
    "verion.modules.identity.adapters",
    "verion.modules.projects.domain",
    "verion.modules.projects.adapters",
    "verion.modules.scanning.domain",
    "verion.modules.scanning.adapters",
    "verion.modules.correlation.domain",
    "verion.modules.correlation.adapters",
    "verion.modules.risk_engine.domain",
    "verion.modules.risk_engine.adapters",
    "verion.modules.brief.domain",
    "verion.modules.brief.adapters",
    "verion.modules.history.domain",
    "verion.modules.history.adapters",
]

[[tool.importlinter.contracts]]
id = "cross-module-correlation"
name = "correlation does not reach into other modules' domain/adapters directly"
type = "forbidden"
allow_indirect_imports = true
source_modules = ["verion.modules.correlation"]
forbidden_modules = [
    "verion.modules.identity.domain",
    "verion.modules.identity.adapters",
    "verion.modules.projects.domain",
    "verion.modules.projects.adapters",
    "verion.modules.scanning.domain",
    "verion.modules.scanning.adapters",
    "verion.modules.normalization.domain",
    "verion.modules.normalization.adapters",
    "verion.modules.risk_engine.domain",
    "verion.modules.risk_engine.adapters",
    "verion.modules.brief.domain",
    "verion.modules.brief.adapters",
    "verion.modules.history.domain",
    "verion.modules.history.adapters",
]

[[tool.importlinter.contracts]]
id = "cross-module-risk-engine"
name = "risk_engine does not reach into other modules' domain/adapters directly"
type = "forbidden"
allow_indirect_imports = true
source_modules = ["verion.modules.risk_engine"]
forbidden_modules = [
    "verion.modules.identity.domain",
    "verion.modules.identity.adapters",
    "verion.modules.projects.domain",
    "verion.modules.projects.adapters",
    "verion.modules.scanning.domain",
    "verion.modules.scanning.adapters",
    "verion.modules.normalization.domain",
    "verion.modules.normalization.adapters",
    "verion.modules.correlation.domain",
    "verion.modules.correlation.adapters",
    "verion.modules.brief.domain",
    "verion.modules.brief.adapters",
    "verion.modules.history.domain",
    "verion.modules.history.adapters",
]

[[tool.importlinter.contracts]]
id = "cross-module-brief"
name = "brief does not reach into other modules' domain/adapters directly"
type = "forbidden"
allow_indirect_imports = true
source_modules = ["verion.modules.brief"]
forbidden_modules = [
    "verion.modules.identity.domain",
    "verion.modules.identity.adapters",
    "verion.modules.projects.domain",
    "verion.modules.projects.adapters",
    "verion.modules.scanning.domain",
    "verion.modules.scanning.adapters",
    "verion.modules.normalization.domain",
    "verion.modules.normalization.adapters",
    "verion.modules.correlation.domain",
    "verion.modules.correlation.adapters",
    "verion.modules.risk_engine.domain",
    "verion.modules.risk_engine.adapters",
    "verion.modules.history.domain",
    "verion.modules.history.adapters",
]

[[tool.importlinter.contracts]]
id = "cross-module-history"
name = "history does not reach into other modules' domain/adapters directly"
type = "forbidden"
allow_indirect_imports = true
source_modules = ["verion.modules.history"]
forbidden_modules = [
    "verion.modules.identity.domain",
    "verion.modules.identity.adapters",
    "verion.modules.projects.domain",
    "verion.modules.projects.adapters",
    "verion.modules.scanning.domain",
    "verion.modules.scanning.adapters",
    "verion.modules.normalization.domain",
    "verion.modules.normalization.adapters",
    "verion.modules.correlation.domain",
    "verion.modules.correlation.adapters",
    "verion.modules.risk_engine.domain",
    "verion.modules.risk_engine.adapters",
    "verion.modules.brief.domain",
    "verion.modules.brief.adapters",
]

[[tool.importlinter.contracts]]
id = "framework-isolation"
name = "domain and application stay framework-free"
type = "forbidden"
source_modules = [
    "verion.modules.identity.domain",
    "verion.modules.identity.application",
    "verion.modules.projects.domain",
    "verion.modules.projects.application",
    "verion.modules.scanning.domain",
    "verion.modules.scanning.application",
    "verion.modules.normalization.domain",
    "verion.modules.normalization.application",
    "verion.modules.correlation.domain",
    "verion.modules.correlation.application",
    "verion.modules.risk_engine.domain",
    "verion.modules.risk_engine.application",
    "verion.modules.brief.domain",
    "verion.modules.brief.application",
    "verion.modules.history.domain",
    "verion.modules.history.application",
]
forbidden_modules = [
    "fastapi",
    "sqlalchemy",
    "redis",
    "arq",
    "uvicorn",
]
