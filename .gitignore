# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# uv / venv
.venv/

# Tooling caches
.ruff_cache/
.pytest_cache/
.mypy_cache/

# Env
.env
infra/.env
# `.env` above matches only a file named exactly `.env`, not `.env.local`.
.env*.local

# Node / Next.js (frontend/, ADR-0031) — added before frontend/ exists, so its
# first `git add` cannot stage either.
node_modules/
.next/

# Editors/OS
.vscode/
.idea/
.DS_Store
